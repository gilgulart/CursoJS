for (let i = 0; i <= 20; i++) {
 const isPeer = i % 2 === 0 ? 'par' : 'ímpar'  ; 
    console.log(i, isPeer);
    
}

const fruits = ["maçã", "pêra", "uva"];

for (let index = 0; index < fruits.length; index++) {
    const element = fruits[index];
    console.log(`Índice ${index}: ${element}`);
    
    
}